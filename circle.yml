machine:
    java:
        version: oraclejdk8

test: 
    override: 
        - mvn test
        - mvn com.gavinmogan:codacy-maven-plugin:coverage -DcoverageReportFile=target/site/jacoco/jacoco.xml -DprojectToken=* -DapiToken=*

general:
    branches:
        only:
            - master

